<?xml version="1.0" encoding="UTF-8"?>
<sitemesh>
	<!-- Decorator cho Admin -->
	<mapping path="/admin/*" name="admin" decorator="admin.jsp" />

	<!-- Decorator cho User -->
	<mapping path="/user/*" name="user" decorator="user.jsp" />
	
	<!-- Decorator cho Vendor -->
	<mapping path="/vendor/*" name="vendor" decorator="vendor.jsp" />

	<!-- Decorator cho Shipper -->
	<mapping path="/shipper/*" name="shipper" decorator="shipper.jsp" />
	
	<!-- Decorator cho Guest -->
	<mapping path="/web/*" name="web" decorator="web.jsp" />

</sitemesh>
